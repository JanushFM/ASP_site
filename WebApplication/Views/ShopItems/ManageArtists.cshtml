@model IEnumerable<Domain.Entities.Artist>

@{
    ViewData["Title"] = "Manage Artists";
}
@if (Model.Any())
{
    <h1>Artists</h1>
    <p>
        <a class="btn btn-primary mb-3" style="width:auto" asp-action="CreateArtist">Create a new one</a>
    </p>
    <table class="table">
        <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                Edit
            </th>
            <th>
                Delete
            </th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>

                <td>
                    <a asp-action="EditArtist" class="btn btn-primary" asp-route-id="@item.Id">Edit</a>
                </td>
                <td>
                    <form method="post">
                        <button asp-action="DeleteArtist" asp-route-id="@item.Id"
                                type="submit"
                                class="btn btn-danger">
                            Delete
                        </button>
                    </form>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
else
{
    <div class="card">
        <div class="card-header">
            No artist created yet
        </div>
        <div class="card-body">
            <h5 class="card-title">
                Use the button below to create an artist
            </h5>
            <a class="btn btn-primary" style="width:auto"
               asp-action="CreateArtist">
                Add new artist
            </a>
        </div>
    </div>
}